/**
 * 【第 4 周】W4_6CountChar — 统计字符
 *
 * ■ 要求
 *   读一个字符串，统计：大写字母、小写字母、数字、空格、其他字符 各几个。
 *
 * ■ 练的是
 *   charAt()、字符比较、多个计数器、Character.isUpperCase()
 *
 * ■ 验收标准
 *   输入 Hello World 123!
 *   大写 2 ｜ 小写 8 ｜ 数字 3 ｜ 空格 2 ｜ 其他 1
 *
 * ■ 提示
 *   既可以手写 c >= 'A' && c <= 'Z'，也可以用 Character.isUpperCase(c)。
 *   两种都试一次，比较哪个更清楚。
 *
 * ■ 动手前先想
 *   1. 需要几个计数器？每个的初始值是什么？
 *   2. 「其他字符」怎么算？（提示：以上四类都不是的，就是其他）
 *   3. 遍历字符串用 for 还是 while？下标从几到几？
 *
 * ■ 陷阱提醒
 *   每个字符只能被算进一类。如果用了多个独立的 if，会不会一个字符被算两次？
 *   （想一想：'A' 会同时命中「大写」和「其他」吗？）
 */
public class W4_6CountChar {
    public static void main(String[] args) {

        // 读字符串，遍历统计，最后打印五个数字

    }
}
